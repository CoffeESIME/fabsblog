import { FFBaseProps } from "../../Utils/generalTypes"

export enum FFMapClass {
  MAIN = "ff-button-main",
}

export enum FFButtonSize {
  SMALL = "small",
  MEDIUM = "medium",
  LARGE = "large",
}

export type FFButtonProps = FFBaseProps & {
};